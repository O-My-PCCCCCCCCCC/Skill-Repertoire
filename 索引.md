# skill 文件夹索引（公开分发）

本文件夹存放可发布的 skill 包。`.skill` 为 zip 格式（UTF-8 文件名），
解压后按各包内 README 使用。

## 目录

| 包 | 是什么 | 怎么用 |
|---|---|---|
| `dsh-mode-status.skill` | **DSH Web 模式状态点插件**：在 DeepSeek Harness Web 侧栏底部显示"梁文谷/梁文峰"时间分段模式的实时状态（模式色圆点+时间+倒计时），点击弹详情 | 解压后运行 `install.ps1`，重启 DSH Web |
| `dsh-web-ui-plugin.skill` | **给 DSH Web 写客户端插件/改 UI 的流程 skill**：不改 DSH 本体注入自定义 UI 的完整方法（DOM 锚点/DSW 主题变量/性能规范/调试） | 解压后读 `SKILL.md`，可复用流程 |
| `meteor-zh.skill` | Minecraft Meteor Client 汉化工具 | 见包内 README |
| `dsh-web-autostart.skill`（zip） | DSH Web 开机自启方案 | 见包内 README |

## 下载方式

直接取对应 `.skill` 文件即可；`.skill` 本质是 zip，可改后缀解压。

## 许可

- `dsh-mode-status` / `dsh-web-ui-plugin`：**MIT**（可自由使用/修改/分发）
- `meteor-zh`：GPL-3.0（个人学习用，遵守原项目许可）
- `dsh-web-autostart`：见包内说明

## 作者

追寻光的影 · 2026-08-19
